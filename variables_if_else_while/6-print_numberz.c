#include <stdio.h>
/**
 * main - entry point of all c programs
 *
 * Return: 0 if successfull
 */
int main(void)
{
	int a;

	for(a = 0; a < 10; a++)
		putchar(a + '0');

	putchar('\n')

	return (0);
}
